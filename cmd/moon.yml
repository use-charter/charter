language: "go"

tasks:
  build:
    command: "bun"
    args:
      - "../scripts/go-build.mjs"
    inputs:
      - "charter/**/*.go"
  test:
    command: "bun"
    args:
      - "../scripts/go-test.mjs"
    inputs:
      - "charter/**/*.go"
