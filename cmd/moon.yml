language: "go"

tasks:
  build:
    command: "bun"
    args:
      - "../scripts/go/go-build.ts"
    inputs:
      - "charter/**/*.go"
  test:
    command: "bun"
    args:
      - "../scripts/go/go-test.ts"
    inputs:
      - "charter/**/*.go"
