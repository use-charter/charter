---
title: "AE-SUPPRESS-002"
description: "An explicit permanent waiver (`expires: permanent`) must name an `approver` who owns the accepted risk; an unapproved permanent waiver is a governance hole, so Charter both flags it (High) and refuses to honor it — the finding re-surfaces until an approver is recorded or a finite `expires` is set. A suppression with no `expires` is a default-TTL suppression (honored) and is not a permanent waiver."
---

**Rule ID:** AE-SUPPRESS-002 · **Severity:** High · **Category:** Governance · **Auto-fixable:** No

## Why this rule

A permanent suppression with no expiry and no approver is an unbounded waiver. It silently removes a finding from the score indefinitely with no human accountability attached.

## What triggers it

Charter audits every active suppression — non-expired entries in `.charter-suppress.yml` and inline `# charter:ignore` directives matched to a finding — and flags any entry that is an explicit permanent waiver without a named approver.

A suppression is a permanent waiver when its `expires` field is set to the literal value `permanent`. If that entry has no `approver:` field (or the approver is blank), Charter flags it High and, critically, **does not honor the suppression** — the underlying finding remains active and is included in scoring. This prevents a single developer from permanently silencing a finding with no accountability attached.

Suppressions with a future `expires` date, or with no `expires` at all (which applies a default TTL), are not permanent waivers and do not require an approver.

## Examples

### Failing

```yaml
- rule: AE-CC-002
  reason: "we'll deal with it later"
  expires: permanent
```

No `approver` — flagged High, and the suppression is not honored.

### Passing

```yaml
- rule: AE-CC-002
  reason: "accepted risk — CI config lives in a separate repo"
  expires: permanent
  approver: security-team
```

A time-bounded suppression with no approver also passes:

```yaml
- rule: AE-MCP-001
  reason: "temporary during migration"
  expires: 2026-09-01
```

## How to fix

Add an `approver:` field naming the person or team who accepted the permanent risk, or convert the entry to a time-bounded suppression by replacing `expires: permanent` with a specific date. The `charter suppress <RULE> --reason "…" --approver "<name>"` command writes both fields for you. To set a finite window instead, use `--expires 90d`.

## Score impact

`High` — deducts 10 points via the standard formula; no cap.

## Edge cases

`expires` present but malformed (not `YYYY-MM-DD`) fails **closed** — the engine treats it as inert so it suppresses nothing and the finding stays active (loud); an inert entry is not audited by this rule (only non-expired file entries and matched inline directives are audited); a blank-only `approver` counts as missing; secrets are suppressible like any rule, so an unapproved permanent `AE-SEC-001/002` suppression fires this rule and is not honored (the ≤ 49 cap holds — an approved permanent secret suppression lifts it); an entry that is both reason-less and permanent-without-approver fires both `AE-SUPPRESS-001` (Medium) and `AE-SUPPRESS-002` (High).

## Related rules

- [AE-SUPPRESS-001](/rules/AE-SUPPRESS-001)
- [AE-SUPPRESS-003](/rules/AE-SUPPRESS-003)

## CLI

```bash
charter explain AE-SUPPRESS-002
```
