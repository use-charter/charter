# Phase 1 Slice 1 Implementation Plan

## Task 3 Correction

- Do not rely on tracked or hand-written `.git/HEAD` fixture content for repository resolution tests.
- `internal/repository/root_test.go` should build its own temporary directory tree with a `.git` marker inside the test.
- `internal/repository/inventory_test.go` should create a temporary git repository and assert inventory behavior through real git-backed file enumeration.
- `internal/repository/inventory.go` should use non-destructive git-backed listing for tracked files plus untracked, non-ignored files, while excluding `.git` internals from results.
