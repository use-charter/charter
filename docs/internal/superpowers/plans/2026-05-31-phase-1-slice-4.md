# Phase 1 Slice 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a structured `Locations` field to `findings.Finding`, capture line numbers in the secret rules, emit `locations` in the JSON contract with a versioned schema, display `path:line` in text output, and replace the duplicated agent-context-file list with one canonical leaf package. Implements ADR-0009 and resolves review findings F2 and M-3.

**Architecture:** Keep the slice deterministic and additive. The JSON change adds an optional `locations` array; existing fields and behavior are unchanged. SARIF, Markdown, `charter fix`, and new rules are explicitly out of scope. Reuse the existing findings, scoring, doctor, and render seams.

**Tech Stack:** Go 1.26.3, existing Cobra-based `charter doctor`, current findings/scoring/render packages, current Git-backed inventory, fixture repos under `testdata/repos/`, root verification via `moon run :check`.

**Out of scope (do not implement here):**

- `--format sarif` (M1.5 — consumes this contract)
- `--format markdown`, `auto_fix`, `guidance` fields (M1.1 T1.1.4 / M1.4)
- new rules; config loading; scoring changes

---

## Precondition: secret-package access

`internal/secrets/` and `internal/rules/secrets/` are blocked for the AI agent by the `fieldnation` Cursor blocklist pattern `**/secrets/**`. Tasks 1 and 3 edit those packages.

- [ ] **Step 0: Confirm access.** Either confirm the `fieldnation` Cursor admin has narrowed/removed `**/secrets/**`, or temporarily rename the secret packages to an unblocked path for the duration of the slice and rename back at the end. Do not start Tasks 1 or 3 until read/write access to the secret packages is confirmed.

## Task 1: Canonical agent-context-file package (M-3)

**Files:**
- Create: `internal/agentcontext/agentcontext.go`
- Create: `internal/agentcontext/agentcontext_test.go`
- Modify: `internal/rules/context/ctx001.go`
- Modify: `internal/rules/secrets/sec001.go`

- [ ] **Step 1:** Create `internal/agentcontext` exporting `Files []string` (the eight single-file candidates in precedence order) and `const CursorRulesDir = ".cursor/rules"`. The package must be a dependency-free leaf.
- [ ] **Step 2:** Replace `supportedContextFiles` in `ctx001.go` with `agentcontext.Files`, and replace the inline `.cursor/rules` literal with `agentcontext.CursorRulesDir`. Preserve current precedence/first-match behavior.
- [ ] **Step 3:** Replace `agentVisibleFileTargets` in `sec001.go` with `agentcontext.Files` + `agentcontext.CursorRulesDir`. Preserve current scan-all behavior.
- [ ] **Step 4:** Add a drift test proving a file added to `agentcontext.Files` is both recognized by the context rule and scanned by the secret rule (one fixture-backed test asserting both paths consume the same list).
- [ ] **Step 5:** Run `go test ./internal/rules/context ./internal/rules/secrets ./internal/agentcontext -count=1`. Expected: pass.

## Task 2: Location type, JSON contract, and schema (F2 / ADR-0009)

**Files:**
- Modify: `internal/findings/finding.go`
- Modify: `internal/render/json/render.go`
- Modify: `internal/render/json/render_test.go`
- Create: `schemas/doctor-result.schema.json`

- [ ] **Step 1:** Add `type Location struct { Path string; Line int }` and a `Locations []Location` field to `findings.Finding` (per ADR-0009). No behavior.
- [ ] **Step 2:** Add a `locations` array (`[]locationDTO{ Path, Line }`) to `findingDTO` and map it in `toFindingDTOs`. Empty slice serializes as `[]`.
- [ ] **Step 3:** Write the JSON test first: assert that a finding with a location serializes `locations[0].path` and `locations[0].line`, and that a finding with no location serializes `locations: []`. Run to confirm failure, then implement Step 2 to green.
- [ ] **Step 4:** Create `schemas/doctor-result.schema.json` describing the top-level result including the new `locations` array; reference ADR-0009 in a comment or sibling note per the `schemas/` contract.
- [ ] **Step 5:** Run `go test ./internal/render/json ./internal/findings -count=1`. Expected: pass.

## Task 3: Line capture and SEC locations (F2 secrets)

**Files:**
- Modify: `internal/rules/secrets/sec001.go`
- Modify: `internal/rules/secrets/sec002.go`
- Modify: `internal/rules/secrets/sec001_test.go`
- Modify: `internal/rules/secrets/sec002_test.go`

- [ ] **Step 1:** In the secret scan loops, switch `for _, line := range ...` to `for i, line := range ...` and record `i + 1` as the matching line.
- [ ] **Step 2:** Populate `Locations: []findings.Location{{Path: target, Line: matchLine}}` on the `AE-SEC-001` and `AE-SEC-002` findings. Keep the existing `Evidence` strings.
- [ ] **Step 3:** Extend the existing secret tests to assert the finding's `Locations[0].Path` and a non-zero `Locations[0].Line`, while still asserting the secret value is redacted in evidence.
- [ ] **Step 4:** Run `go test ./internal/rules/secrets -count=1`. Expected: pass.

## Task 4: Locations in the remaining rules (F2 breadth)

**Files:**
- Modify: `internal/rules/ci/ci002.go` (+ test)
- Modify: `internal/rules/context/ctx001.go`, `ctx002.go`, `ctx004.go` (+ tests)

- [ ] **Step 1:** Populate file-level `Locations` (Line `0`) where a single file is implicated: `AE-CI-002` (the offending workflow file when applicable), `AE-CTX-001` present-but-weak (the resolved context file), `AE-CTX-002` (the context file), `AE-CTX-004` (`.gitignore` when present). Leave absence findings (`AE-ENV-001`, `AE-CTX-001` when no context file exists) with empty `Locations`.
- [ ] **Step 2:** Update the affected rule tests to assert the expected `Locations` (including the empty-array cases).
- [ ] **Step 3:** Run `go test ./internal/rules/... -count=1`. Expected: pass.

## Task 5: Text rendering and doc/spec alignment

**Files:**
- Modify: `cmd/charter/doctor.go` (text output)
- Modify: `docs/internal/specs/AE-SEC-001.md`, `AE-SEC-002.md`, `AE-CI-002.md` (note structured location)
- Modify: `docs/internal/superpowers/specs/2026-05-29-phase-1-slice-2-design.md` (cross-link the additive `locations` field)
- Verify: `docs/internal/architecture/charter-architecture-2026.md` output examples remain consistent

- [ ] **Step 1:** In the text renderer, print `path:line` for each located finding (and `path` when `line == 0`); keep the existing summary and score lines.
- [ ] **Step 2:** Add a text-output test asserting `path:line` appears for a located finding.
- [ ] **Step 3:** Update the affected `AE-*` specs to state that the finding carries a structured location, and cross-link ADR-0009 from the Slice 2 design's output-contract section.
- [ ] **Step 4:** Confirm the architecture-doc output examples (`AGENTS.md:14`, `.mcp.json:7`) now match real output; adjust only if the implementation forces a wording change.

## Task 6: Final slice verification

- [ ] **Step 1:** Run `mise x -- moon run :check`. Expected: pass.
- [ ] **Step 2:** Run `go run ./cmd/charter doctor --path . --threshold 80` and `--format json`; confirm the score is unchanged and `locations` is present and schema-valid.
- [ ] **Step 3:** If the secret packages were temporarily renamed in Step 0, rename them back and re-run `moon run :check` to confirm green.
- [ ] **Step 4:** Update `AGENTS.md` / `README.md` state lines to reflect Slice 4 only if implementation changes the documented current state.

## Notes for M1.5 (SARIF), not this slice

- The SARIF renderer projects `Locations` onto `result.locations[].physicalLocation` (`artifactLocation.uri` = `path`, `region.startLine` = `line`). No further location data is stored; richer SARIF regions are derived at render time.
- `auto_fix` / `guidance` JSON fields and `charter fix` land in M1.4 and reuse the same `Locations`.
