---
import Section from './Section.astro';
---
<Section id="problem" title="The Problem">
  <div class="problem__body reveal">
    <p class="problem__lead">Teams adopting coding agents don't fail because the model is bad — they fail because the repo isn't ready.</p>
    <p class="problem__detail">Unpinned MCP servers. Secrets visible to agents. No verification command. Outdated AGENTS.md. These aren't policy violations; they're operational gaps that block agent adoption.</p>
  </div>
</Section>
