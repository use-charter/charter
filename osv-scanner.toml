# Charter OSV-Scanner suppression configuration.
#
# No active suppressions. This file is referenced by `osv-scanner --config`
# (see moon.yml :security) and is kept as the home for any future, justified
# IgnoredVulns entries.
#
# History:
# - GHSA-48c2-rrv3-qjmp (yaml@2.7.1, transitive via yaml-language-server in
#   @astrojs/check): removed 2026-06-14. Superseded by the `yaml: ">=2.8.3"`
#   override in web/package.json, which resolves yaml to a patched 2.9.0 — the
#   vulnerability is fixed at the source, so the suppression became an unused
#   ignore. Prefer fixing over suppressing.
